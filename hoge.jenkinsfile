echo env.GIT_COMMIT

node {
    stage('checkout') {
        def scmVars = checkout scm
        echo "aaaa"
        echo scmVars.GIT_COMMIT
    }
    stage('hoge') {
        echo env.GIT_COMMIT
    }
}
